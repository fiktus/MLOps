[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error::sklearn.exceptions.InconsistentVersionWarning"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]